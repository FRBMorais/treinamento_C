/*

FONTE: Felipe

*/

/*

Retorna uma máscara com os n bits de menor peso 
em 1 (todos os outros 0).

Máscaras de bits são fundamentais porque permitem selecionar, 
limpar ou manter apenas as posições de bit que você deseja em uma palavra. 
Ao combinar uma máscara com o operador AND (&), 
você cancela (zera) todos os bits onde a máscara tenha 0 
e preserva só aqueles onde a máscara tenha 1. 
Já o operador OR (|) com uma máscara de 1s garante que certos bits fiquem setados, 
e o XOR (^) faz o toggle.

*/